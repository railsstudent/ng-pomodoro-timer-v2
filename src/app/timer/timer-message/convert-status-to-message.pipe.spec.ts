import { ConvertStatusToMessagePipe } from './convert-status-to-message.pipe'

describe('ConvertStatusToMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertStatusToMessagePipe()
    expect(pipe).toBeTruthy()
  })
})
